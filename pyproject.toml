[project]
name = "flight-delay-mlops"
version = "0.1.0"
description = "Production-oriented ML/MLOps project for flight delay prediction."
requires-python = ">=3.10,<3.11"
dependencies = [
    "pandas>=2.3.3",
]
